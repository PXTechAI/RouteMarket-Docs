---
title: "Omni 兼容视频生成"
description: "Omni 旧版 /v1/videos 文生视频、图生视频和视频修改接口"
---

本页整理 RouteMarket 的 **Omni 兼容视频生成** 兼容接口，包括可识别的端点、结构化参数和请求示例。

> 兼容性提示：页面由公开协议中的技术字段生成。具体模型、额度和路由能力以 RouteMarket 控制台及实际网关响应为准。

## 参数参考

### 参数表 1

| 能力 | 方法 | 路径 |
| ---- | ------ | ---------------------- |
| 提交任务 | `POST` | `/v1/videos` |
| 查询任务 | `GET` | `/v1/videos/{task_id}` |

### 参数表 2

| 模型 | 时长 | 分辨率 |
| ---------------- | ---- | ---- |
| `omni_flash-10s` | 10 秒 | 720p |

### 参数表 3

| 模式 | 传参方式 |
| ---- | ------------------------ |
| 文生视频 | 只传 `prompt`，不传参考素材 |
| 图生视频 | 传 1 到 7 个图片 URL |
| 视频修改 | 在参考素材中传视频 URL，并可搭配图片 URL |

### 参数表 4

| 字段 | 类型 | 说明 |
| ----------- | ------ | ------------------------------------------ |
| `id` | string | - |
| `status` | string | 相关值：`queued`、`processing`、`completed`、`failed` |
| `progress` | number | - |
| `video_url` | string | - |
| `error` | object | - |

### 参数表 5

| 项目 | 说明 |
| ------ | --------------------------------- |
| 参考素材数量 | 相关值：`7` |
| 文生视频 | 相关值：`images`、`input_reference[]` |
| 图生视频 | - |
| 视频修改 | - |

## 请求与响应示例

### 示例 1

```text
Authorization: Bearer $ROUTEMARKET_API_KEY
```

### 示例 2

```json
{
  "id": "task_xxx",
  "status": "queued",
  "model": "omni_flash-10s",
  "created_at": 1760000000
}
```

### 示例 3

```bash
curl https://api.routemarket.ai/v1/videos/task_xxx \
  -H "Authorization: Bearer $ROUTEMARKET_API_KEY"
```

### 示例 4

```json
{
  "id": "task_xxx",
  "status": "completed",
  "progress": 100,
  "video_url": "https://api.routemarket.ai/videos/task_xxx.mp4"
}
```

## 接入说明

- API 基础地址：`https://api.routemarket.ai`
- 推荐密钥环境变量：`ROUTEMARKET_API_KEY`
- 请求使用 `Authorization: Bearer <API_KEY>`。
- 上线前请用实际账号验证端点、模型名、限额与异步任务状态。

{/* migration-source-path: /docs/api/omni */}
